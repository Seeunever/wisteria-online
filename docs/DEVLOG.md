# 开发日志

本文件用于家里电脑、公司电脑和不同 Codex 会话之间的工程交接。最新记录放在最上方；旧记录只追加、不改写。

禁止在这里记录公网 IP、SSH 用户、私钥路径、口令、原始剧本文件名、OCR 内容、角色秘密、线索正文或剧情摘要。

## 2026-08-29 — 选角卡片改用席位独立封面

- 起始提交：`18e8189`；本轮改动尚未提交或发布。
- 数据映射：六个选角席位分别使用各自角色册第一页的公开剪影封面；统一关系图不再作为席位简介，封面也不在角色私密正文中重复展示。
- 页面：选角区改为桌面三列、窄屏两列、手机单列的纵向卡片；封面保持原始竖版比例，选择按钮占满卡片宽度。
- 回归保护：实际包 HTTP 冒烟新增“每席位必须有图片、六张图片必须互异、页面必须实际引用”的内容盲校验。
- 验证：新冻结包安全报告为 `validated`、`freeze_ready: true`、零阻断、零警告；运行时测试 23/23、Lint 和实际包 HTTP 权限矩阵通过；浏览器在 1280×720 与 390×844 下确认六张封面均加载、无横向溢出、无控制台错误。
- 隔离审计：`PRIVATE_ROLE_COVER_AUDIT_PASSED`；生产环境仍是上一安全版本，尚未切换。
- 唯一下一步：隔离审计通过后再部署，并在公网只复核图片数量、响应状态与布局边界。

## 2026-08-29 — 二次剧透事故处置：页内与多页混合边界

- 起始提交：`18e8189`；本轮改动尚未提交。
- 事故：上一轮只隔离了独立结局图片，玩家说明投影仍引用了混合扫描源中的结局区域；发现后立即停止生产服务并确认应用端口无监听。
- 根因：内容复核范围错误地缩小到附件组，没有逐个复核最终 `playerGuide` 投影的物理页、页内裁切和混合多页边界；原有运行时测试只能证明已标记 L3 对象不可读，无法发现另一个 L1 来源中的重复结局内容。
- 修复：使用全新私有运行重新盘点与映射；玩家说明逐页、逐裁切复核，结局封面和后续材料从 `playerGuide` 移除，独立结局材料继续只受 `session_completed` 释放控制。
- 验证：新安全报告 `validated`、`freeze_ready: true`、零阻断、零警告；本机与 Linux 隔离实例实际包 HTTP 权限矩阵通过；隔离内容审查返回 `PRIVATE_PLAYER_GUIDE_AUDIT_PASSED`。
- 生产：保留原用户和等待中房间，将等待中房间原子迁移到新冻结版本并清除旧角色选择；旧版本已退役且没有房间引用，旧数据完整备份可回滚；服务与公网 HTTPS 恢复正常。
- 唯一下一步：刷新已有页面并重新进入等待中的房间，确认玩家说明只包含经复核区域；旧标签页已经下载的图片不会被服务器远程抹除。

## 2026-08-29 — 结局材料隔离与结束后解锁

- 起始提交：`18e8189`；本轮改动尚未提交。
- 安全事件：发现同一附件容器中的玩家说明与结局材料被错误归入同一公开指南；立即停止生产服务，未在错误版本上继续开放访问。
- 拆解规则：page map 新增独立 `resolution` 区域；物理相邻或同目录不再推导相同可见性，结局页必须来自 `solution` 来源并声明 `session_completed` 释放条件。
- 运行时：L3 内容继续被通用读取接口永久拒绝；只有已加入、房间已结束、授权版本一致且来源污染链完整的结局投影可以读取对应图片，进行中直链返回 404。
- 页面：真相快捷入口与内容只在房间结束后出现；游戏说明继续作为玩家公开规则提供，不包含结局页。
- 测试修复：实际包 HTTP 冒烟新增进行中拒绝、结束后释放和图片私有缓存检查；删除房间测试拆分 HTTP、数据库和级联阶段，修正日志文件缺失造成的错误阶段报告。
- 验证：私有安全报告 `validated`、`freeze_ready: true`、零阻断、零警告；拆解器测试 76 项通过（1 项按环境跳过）；运行时测试 23/23、Lint、生产构建和 Skill 快速校验通过；本机与 Linux 隔离实例的实际冻结包 HTTP 权限矩阵均通过。
- 生产：旧代码和旧数据完整保留用于回滚；上线后为 0 用户、0 房间、1 个冻结剧本，服务与公网 HTTPS 均返回正常状态。
- 唯一下一步：由真实设备创建测试房，仅核对游戏说明入口存在且真相入口在游戏结束前不存在；不要为验证而提前结束正式游戏房。

## 2026-08-29 — 页图优先冻结包与生产部署

- 起始提交：`18e8189`；本轮改动尚未提交。
- 拆解技能：新增页图优先的私有 page map、确定性 bundle 编译器、预检和合成回归测试；原始页统一预渲染为私有 WebP 对象，网站运行时只按授权提供经过验证的页面或裁切，不把原始包、OCR 或完整 bundle 放进仓库和静态产物。
- 玩法结构：冻结包包含完整角色席位、阶段、地点、线索、公开游戏说明和选角前公开简介；调查流程由服务端执行全员地点投票、席位轮转取线索、阶段配额、私藏上限、公开义务和完成同意。
- 安装与诊断：安装器和实际包 HTTP 冒烟只输出固定安全阶段码；测试脚本显式初始化隔离数据库，避免把“尚未建表”误报成剧本权限失败。
- 构建修复：standalone 打包显式带入 Sharp 的平台运行时依赖，修复角色页已经生成内容入口但图片请求因缺少原生组件返回 404 的问题；生产发布改为服务器原生 Linux 构建。
- 生产数据：切换前写入数据库备份，旧数据目录整体保留为可回滚副本；正式库已清空用户、会话、设备身份、房间与旧剧本，仅注册一个用户指定公开名称的冻结版本。
- 验证：确定性安全报告为 `validated`、`freeze_ready: true` 且零阻断；拆解器测试 75 项通过（1 项按环境跳过）；`npm run test:runtime`（22/22）、`npm run lint`、`npm run build`、技能快速校验和 `git diff --check` 通过；实际冻结包在本机与生产 Linux 隔离副本上的多身份 HTTP 权限矩阵均通过；公网 HTTP 正确跳转 HTTPS，HTTPS 首页返回 200。
- 唯一下一步：使用真实玩家设备创建新房，确认每台设备显示自己的用户名后进行一次不打开剧情内容的多人开场彩排。

## 2026-08-29 — 说明书驱动的集体调查流程与固定快捷入口

- 起始提交：`18e8189`；本轮改动尚未提交或发布。
- 游戏说明：房间成员可查看仅由已验证 `player_rules` 来源污染的 L1 说明内容；未加入成员、未验证规则来源和其他密级内容继续默认拒绝。
- 固定入口：房间页新增固定在视口底部的“游戏说明 / 公开线索”快捷入口；390×844 合成手机视口滚动前后边界位置一致，两个目标区均可见。
- 调查流程：新增可选的冻结包 `investigationFlow`，支持全员投票、未调查地点过滤、席位游标平票处理、按席位一次一张轮转取得和按轮私藏数量触发公开义务；所有投票、轮次、游标、取得和义务状态均由服务端事务判定。
- 数据：新增调查轮次、投票、取得与授权版本绑定的阶段完成同意账本；旧冻结包未声明 `investigationFlow` 时继续使用原搜索行为。
- 校验器：结构验证器和安全报告白名单同步支持新流程，并阻断空地点、旧主持发牌混用、查看者局部条件造成的席位死锁，以及未列入公开面的内容授权；没有把任何真实规则取值或剧本语义写入公开仓库。
- 权限加固：房间候选由每位已分配玩家的独立授权上下文取并集，玩家限制不再污染全房间结算；图片直链必须同时存在于当前授权投影；公开线索事务校验版本并追加审计事件。
- OCR：项目外私有环境已安装并验证 RapidOCR、ONNX Runtime 与 PyMuPDF；合成中文扫描 PDF 精确识别通过。提取器改为记录运行时真实依赖版本，不再硬编码后端版本；真实包 OCR artifact 仅写入私有运行区。
- 浏览器验证：390×844 合成手机房间无横向溢出；三张地点投票卡正常出现；单人合成投票能结算到席位轮转页面，展开地点后四张线索按钮可用。
- 验证：`npm run test:runtime`（22/22）、`npm run lint`、`npm run build`、`git diff --check` 通过；拆解器 Python 测试 71 项通过（1 项按环境跳过）。
- 阻断：本轮私有候选包生成返回固定安全阻断码，确定性 bundle 报告与安全报告均未成立；没有安装、部署或改动生产数据。
- 唯一下一步：放弃本轮私有运行区；只有在新的隔离运行重新完成验证与实际包权限矩阵后，才可请求生产安装与部署授权。

## 2026-08-29 — 重置生产测试状态并保留单一剧本版本

- 生产数据：在新备份完成后清空全部房间、房间进度、用户、会话和设备身份。
- 剧本版本：严格按一删一留校验移除公开名称带“冻结”的版本；保留版本的安全公开标签改为用户指定名称。被移除的私密版本移入仅管理员可访问的恢复备份，不再由应用注册表引用。
- 安全性：清理期间暂停应用服务；数据库删除、版本删除和标签更新在同一事务内完成，文件移动在事务失败时可回退。
- 验证：事务内确认房间与用户归零、版本注册表只剩一个且标签正确；服务重启后本机健康检查通过。
- 唯一下一步：使用新设备身份进入网站，创建新房并重新测试保留的剧本版本。

## 2026-08-29 — 当前玩家搜查权限与房间公开线索板

- 起始提交：`18e8189`；本轮改动尚未提交。
- 权限语义：新增当前查看者范围的条件求值，地点、候选线索、取得条件和公开条件不再误用房间内全部已分配角色；房间级阶段与主持释放仍保留全局角色语义。
- 搜证与看板：地点继续先展开再选择稳定编号线索；未公开线索只保留个人入口，已公开线索在房间公共看板直接展开，已加入但未分配角色的成员也能查看已授权公开内容。
- 回归覆盖：新增“另一成员的角色不能污染当前玩家地点限制”和“已公开线索可向所有房间成员直接投影”测试；实际冻结包 HTTP 冒烟增加房间公共看板正文断言。
- 发布装配：standalone 构建明确剔除被追踪的本地 `.data`；Windows 发布包会实体化构建链接，Linux 原生构建保留 pnpm 链接。最终采用服务器原生构建发布，数据库先备份，实际冻结包权限冒烟与 HTTPS 健康检查通过。
- 验证：`npm run test:runtime`（16/16）、`npm run lint`、`npm run build`、干净发布产物检查、实际冻结包 HTTP 权限冒烟及生产 HTTPS 健康检查通过。
- 遗留：实际剧本原始来源当前不可用，因此具体的角色—地点映射、搜查顺序与次数尚未生成新的冻结版本；本轮没有修改或覆盖已有剧本包。
- 唯一下一步：重新提供原始剧本文件夹，在全新私有运行区完成规则核对、冻结验证、追加安装与多身份矩阵。

## 2026-08-29 — 两层搜证、成员房间管理与设备自动身份

- 起始提交：`4592cb3`。
- 结束提交：本条记录所在提交。
- 搜证交互：运行中先展开当前地点，再选择一张尚未取得的稳定编号线索背面；服务端重新验证地点、阶段、取得条件、剩余次数和未被占用状态，成功后进入独立阅读页。
- 阅读决策：房间页只显示已取得或已公开线索的索引，不再预先渲染全部线索正文；独立阅读页提供“暂时隐藏”和“公开到全房间”，未持有且未公开的身份继续得到同形不可用响应。
- 房间管理：所有仍在房间内的成员都可刷新页面状态，并在二次确认后删除房间；非成员仍不能删除，外键级联继续清理本局状态。
- 设备身份：有效设备凭据可在会话失效后直接恢复原玩家身份；首页、大厅和房间页直接显示当前玩家名，不再要求重复登录。
- 手机端验收：使用完全合成、不含真实剧本内容的运行数据在 390px 视口完成创建、选角、开场、地点展开、线索取得、隐藏与重新打开；线索按钮保持横排，无横向溢出。
- 验证：`pnpm run test:runtime`（14/14）、`pnpm run lint`、`pnpm run build` 与 `git diff --check` 通过；设备凭据单独恢复身份通过。
- 唯一下一步：备份生产数据库，发布应用代码后对实际冻结包执行有界 HTTP 权限冒烟与 HTTPS 健康检查。

## 2026-08-28 — 完整拆解包安装与生产验收

- 起始提交：`afdcee94a36e07787206060368b678b64d2db738`
- 结束提交：本条记录所在提交。
- 私有剧本：新的完整结构化版本已通过冻结验证并追加安装；安全公开标签与旧测试版本区分，创建新房时可明确选择。既有房间仍固定使用创建时选择的不可变版本。
- 有界投影审计：逐项验证全部角色席位、阶段和线索状态的授权投影；审计只比较结构与可见性结果，不输出或持久化剧情正文。
- 真实 HTTP 冒烟：在生产数据的在线备份与私有对象硬链接副本上启动隔离服务，覆盖公开简介、本人角色页、未授权与不存在资源的同形响应、线索持有/公开状态、房主删除、非房主拒绝和级联清理；临时身份、房间、进程与目录均已清理。
- 生产发布：服务已切换到包含房间删除、阅读状态、选角简介和开场提示的版本；HTTPS 首页、房间入口与静态资源健康，服务无重启。
- 可复用工具：新增 `scripts/test-installed-pack-http.ts`，以后每个新剧本安装后都可执行同一套有时限、可清理且不泄露剧情的 HTTP 权限冒烟，替代此前容易超时的逐图片重复读取方案。
- 验证：私有冻结包验证无阻断项；有界实际包投影审计通过；隔离 HTTP 冒烟通过；`pnpm run test:runtime`（14/14）、`pnpm run lint`、`pnpm run build` 与 `git diff --check` 通过；生产 HTTPS 返回 200。
- 唯一下一步：明日开局前创建一个新房并选择标有“完整拆解版”的版本，完成一次多人设备的无剧情开场彩排。

## 2026-08-28 — 房间删除、阅读状态与选角简介投影

- 起始提交：`5caa67edbadc041be45fe41be68ec031fb3363ca`
- 结束提交：本条记录所在提交。
- 房间管理：新增房主专属删除操作，并要求展开说明、勾选确认后提交；删除使用数据库外键级联清理本局成员、选角、阶段、线索和事件，非房主请求保持无效。
- 阅读状态：房间头部明确区分未选角、已选角等待开场、游戏进行中与已结束；选角后说明第一阶段个人剧本会在开场时解锁，开场后若授权投影仍为空则显示阻断提示，不再用模糊空白掩盖问题。
- 选角简介：大厅只投影冻结包中明确标记为公开简介、在未分配角色状态满足条件且对房间成员有验证授权的内容；个人角色、阶段、线索与主持内容不能作为简介降级公开。缺失公开简介时如实提示数据未录入。
- 验证：`pnpm run test:runtime`（14/14）、`pnpm run lint`、`pnpm run build` 与 `git diff --check` 通过。
- 私有剧本：新的结构化版本正在隔离侧补齐公开选角简介；在实际包访问矩阵通过前不安装或发布。
- 唯一下一步：验证新的冻结包确有全部角色的来源可追溯公开简介，再执行追加安装、实际包多身份矩阵与生产发布。

## 2026-08-28 — 当前房间列表与开场入口可见性

- 起始提交：`e1203c2123d92210cb1de9aca5cabea5d2c43072`
- 结束提交：本条记录所在提交。
- 证据：大厅只查询当前用户已经加入的房间，并要求手工输入六位码；线上服务用户无法读取私密冻结包，房间页又静默吞掉存储异常，导致选角色区和开场区一起消失。
- 修改：登录后的大厅列出全部未结束房间；未加入的等待中房间可直接点击加入，已开场房间只向原成员提供继续入口，继续禁止开场后补员。
- 开场提示：房主始终看到满席开场、少人二次确认或“先选角色”的明确状态；其他玩家看到“等待房主开始游戏”，玩家列表标记房主与选角状态；剧本读取失败改为可见的通用错误。
- 私密存储：线上冻结包恢复为仅服务账户可读的目录和文件权限；安装器拒绝由数据目录所有者之外的账户写入，避免后续安装再次生成服务不可读文件。
- 权限边界：全站房间列表只公开房间码、安全公开剧本标签、状态、人数和创建日期，不返回成员姓名、角色、线索或剧情；未加入身份仍不能打开房间或读取保护内容。
- 验证：`pnpm run test:runtime`（14/14）、`pnpm run lint`、`pnpm run build` 与 `git diff --check` 通过；线上运行账户固定结构检查返回 `PACK_RUNTIME_LOAD_OK`。
- 唯一下一步：完成当前生产构建的房间发现与未加入访问回归后发布。

## 2026-08-28 — 创建时选剧本与少人测试开场

- 起始提交：`92bcbb2a22a192d9ee3e4a7def08429f17eacca6`
- 结束提交：本条记录所在提交。
- 证据：旧流程先创建空房、再在房内选择版本；开场则要求剧本全部角色席位均已认领，单人测试无法继续。
- 修改：创建表单列出安全公开标签，并在同一事务内创建房间、锁定所选冻结版本和加入房主；旧空房仍保留房内装载入口。
- 少人开场：至少一个有效席位后，房主必须先展开风险说明、再勾选确认并提交；事务校验授权版本与角色子集，记录不含剧情的强制开场事件。
- 权限边界：强制开场事件只让阶段入口/完成条件把全部席位视作已到场；角色、地点、线索与主持内容仍只按真实分配和真实事件授权，空缺角色内容不会分给房主、已分配玩家、未分配身份或已离场身份。
- 保持不变：已创建房间继续固定在原不可变版本；开场只统计有效成员的席位，运行中拒绝新加入与离场重入，不能补选空缺角色；非到席类的阶段条件与全部内容授权规则不放宽。
- 验证：`pnpm run test:runtime`（14/14）、`pnpm run lint`、`pnpm run build` 与 `git diff --check` 通过；独立临时数据库与合成双席位剧本完成 HTTP 回归，覆盖创建选版、零席位阻断、二次确认、少人开场和缺席角色不可见；实际冻结包多身份访问矩阵返回 `PRIVATE_ACCESS_MATRIX_PASSED`。
- 唯一下一步：提交并发布本次修改。

## 2026-08-28 — 修复用户名提交停在进行中

- 起始提交：`25f927f34c8920fc4cb80b3c93a58d01662d5735`
- 结束提交：本条记录所在提交。
- 证据：生产端连续收到用户名入口 POST 并返回 303，但浏览器没有继续请求重定向目标，数据库也没有新增身份；本地浏览器复现确认，`Referrer-Policy: no-referrer` 令表单 POST 携带 `Origin: null` 与 `Sec-Fetch-Site: same-origin`，旧校验拒绝后又错误跳转到内部监听地址，最终被 CSP 阻止。
- 根因：同源校验不接受原生表单将 `Origin` 省略或序列化为 `null` 的情况，也没有按可信代理提供的协议和 Host 还原外部 origin；同时，前端在提交事件内立即禁用 submitter，使失败后页面永久显示“正在进入”。
- 修改：有效的显式 `Origin` 继续严格比对；省略或为 `null` 时只接受浏览器提供的 `Sec-Fetch-Site: same-origin`，并用 Host 与代理协议生成外部跳转地址。无效输入仍显示行内错误，有效提交不再提前锁死按钮。
- 保持不变：身份、会话、房间和私密内容授权逻辑未修改。
- 验证：`pnpm run test:runtime`（13/13）、`pnpm run lint`、`pnpm run build` 与 `git diff --check` 通过；真实浏览器表单已从入口跳转到预置占用身份的错误页并恢复按钮；同源 `Origin: null` 成功路径返回 `/rooms`，同时设置会话与设备凭据。
- 唯一下一步：发布后复验线上入口与静态资源。

## 2026-08-28 — 修复首页入口无反馈

- 起始提交：`f511e1d1f4b478b803eaf7c7310ad9a727b79b72`
- 结束提交：本条记录所在提交。
- 根因：生产服务从 standalone 目录启动，但部署产物没有把 `_next/static` 与 `public` 复制进去，导致客户端脚本和样式资源返回 404；缓存可能让页面仍有样式，却没有交互。桌面端页内锚点和空值反馈也不够明确。
- 修改：“开始游戏”改为“填写玩家名”，点击后平滑定位并聚焦输入框；空值或非法格式在提交前显示行内错误并保持焦点；有效提交显示进行中状态。
- 构建：新增 standalone 资源装配脚本，`pnpm run build` 会安全刷新 standalone 内的静态资源与公共视觉资产，防止后续部署再次漏装。
- 保持不变：用户名设备绑定、服务端会话、房间与剧本内容授权逻辑未修改。
- 验证：生产静态资源 11/11 返回 200；真实浏览器确认首屏入口会聚焦输入框，空用户名会显示明确提示；`pnpm run test:runtime` 通过，8/8；`pnpm run lint`、`pnpm run build` 与 `git diff --check` 通过。
- 唯一下一步：妹妹刷新生产页并实际输入一个玩家名进入。

## 2026-08-28 — 用户名设备身份入口

- 起始提交：`9cc5818dae1755b286515e3219e0ccfd669ae1ae`
- 结束提交：本条记录所在提交。
- 目标：朋友小范围使用时取消密码输入，只保留用户名入口，同时继续阻止陌生设备冒认其他玩家并读取其私密角色内容。
- 修改：首次输入用户名即创建身份并签发一年期设备凭据；会话退出或过期后，同一设备仍可只凭原用户名重新进入。
- 修改：用户名一经绑定，其他设备不能直接使用；同一设备也不能悄悄切换到另一身份。用户名支持 1–24 个中文、字母、数字、空格、下划线或连字符。
- 数据库：新增 `device_credentials` 表；保留原有用户列以兼容既有数据库，不再从界面或认证接口接收密码。
- 安全边界：设备凭据仅存于 `HttpOnly`、`SameSite=Strict` Cookie，生产环境同时使用 `Secure`；角色和线索授权继续依据服务端会话。
- 部署：用户名入口沿用认证接口限速；旧密码端点仅兼容重定向，不再接收或验证密码。
- 验证：`pnpm run test:runtime` 通过，8/8；`pnpm run lint` 通过；`pnpm run build` 通过；`git diff --check` 通过。
- 唯一下一步：备份生产数据库，部署本提交，并用无剧情测试身份验证首次进入、同设备重入与陌生设备拒绝。

## 2026-08-28 — 阶段 4 房间大厅改版交接（待完成验收）

- 基线提交：`97a76c6adf52b8b6601244cd7cd0bee324b4361a`
- 当前交接提交：`173068148ed2c419f01ee321603d43cd051b0316`
- 目标：只重构房间大厅的视觉与响应式布局，不修改认证、创建房间、加入房间或房间列表的服务端行为。
- 已完成：重写大厅页面并新增独立 CSS Module；突出最近房间，整理创建 / 加入操作区，并覆盖电脑、Pad、手机断点与通用错误状态。
- 保持不变：账号会话、创建房间、加入房间和房间列表仍沿用原有接口与数据结构；本轮没有提交任何真实剧本内容。
- 自动验证：`git diff --check` 通过；安全关键词扫描通过；`pnpm run lint` 通过；`pnpm run build` 通过。
- 未完成：尚未完成登录态大厅在 1440px、768px、390px 和 320px 下的浏览器视觉验收；隔离测试环境的登录流程没有成功进入大厅，也没有实际提交创建或加入操作。
- 唯一下一步：家里电脑拉取后先完成四个宽度的大厅视觉验收，只修复发现的界面问题；大厅验收前不要开始游戏房间改版。

## 2026-08-28 — 完成阶段 3 原创视觉资产并接入首页

- 起始提交：`97a76c6adf52b8b6601244cd7cd0bee324b4361a`
- 结束提交：`173068148ed2c419f01ee321603d43cd051b0316`
- 目标：只生成并接入通用宅邸氛围、透明紫藤和轻纹理，不修改认证、房间或线索业务逻辑。
- 修改：新增宅邸冬园横幅、透明紫藤枝饰与深紫轻纹理三项原创 WebP，总发布体积约 349 KB；生成源没有进入仓库。
- 修改：首页首屏使用横幅背景与独立渐变保证文字对比度；透明枝饰替代临时 SVG；纹理以低透明度应用于首屏和全页底色。
- 修改：手机标题加入只在窄屏生效的语义断行，避免“暗格 / 里”和“公 / 开”之间出现难看的单字断行。
- 文档：新增 `docs/ART_ASSETS.md`，保存三个最终 Prompt、文件规格、安全边界和再生成复核方法；README 更新到阶段 3。
- 图像验证：透明枝饰优化前后均有真实 alpha，发布文件 alpha 范围为 0–255；纹理原图左右与上下边平均绝对色差约为 3.75/255 与 3.97/255。
- 自动验证：`git diff --check` 通过；`pnpm run lint` 通过；`pnpm run build` 通过。
- 浏览器验证：1440px 电脑和 768px Pad 首屏资产加载、构图与表单对比度通过且无横向溢出；390px 与 320px 手机无横向溢出，窄屏标题保持四行语义断句，账户入口与输入框高度为 44px。
- 未运行：未运行 `pnpm run test:runtime`，因为本阶段没有修改权限、数据或房间运行时逻辑；未提交任何表单。
- 已知风险或阻塞：大厅和游戏房间仍使用旧布局；开发模式在内嵌浏览器中会记录 React 调试所需 `eval()` 不可用的环境提示，生产构建不使用该路径，本轮没有放宽 CSP。
- 唯一下一步：请妹妹验收阶段 3；随后阶段 4 只改造房间大厅，不修改游戏房间。

## 2026-08-28 — 完成阶段 2 响应式首页与账户入口

- 起始提交：`afcbf7c3b3fcf478700f29a8cee86f63cd02aeba`
- 结束提交：尚未提交。
- 目标：把阶段 1 设计落到真实首页，保留现有登录、注册、退出和房间跳转行为，不修改房间业务逻辑。
- 修改：重写 `app/page.tsx`，建立紫藤深宅主题首屏、已登录状态、三步玩法说明、公开线索预览、流程说明和权限保证区。
- 修改：新增 `app/auth-panel.tsx`，以可访问页签切换登录和注册表单，并继续提交到现有认证接口；失败信息保持通用，不暴露账号是否存在。
- 修改：新增 `app/home.module.css`，隔离首页样式，并覆盖电脑、Pad、390px 手机和 320px 最小宽度；公共装饰仅使用 CSS 与内联 SVG。
- 修改：更新全局主题色和浏览器主题色；未生成图片，未添加依赖，未修改认证、房间、角色、线索或数据库逻辑。
- 修复：Pad 点击账户锚点时标题曾被粘性导航遮挡，已加入滚动安全间距。
- 修复：320px 视口曾因页面 `min-width` 与滚动条共同产生 15px 横向溢出，已移除该限制；复测 `scrollWidth === clientWidth === 305px`。
- 浏览器验证：1440px 电脑端首屏双栏、登录 / 注册页签通过；768px Pad 表单三列重组且无横向溢出；390px 手机单栏与页签通过；320px 无横向溢出且品牌入口、账户入口、页签和输入框触控高度均为 44px。
- 自动验证：`pnpm run lint` 通过；`pnpm run build` 通过；`pnpm run test:runtime` 通过，7/7；`git diff --check` 通过。
- 未运行：没有在浏览器中实际创建或登录测试账号，避免向本地数据库写入额外测试身份；认证接口实现本轮未改动。
- 已知风险或阻塞：当前仍使用通用 CSS / SVG 氛围装饰，尚未接入阶段 3 原创位图；大厅和游戏房间仍是旧布局。
- 唯一下一步：妹妹验收真实首页后，阶段 3 只生成并接入无剧情的宅邸氛围、透明紫藤和轻纹理资产。

## 2026-08-28 — 完成阶段 1 视觉规范与三端线框

- 起始提交：`afcbf7c3b3fcf478700f29a8cee86f63cd02aeba`
- 结束提交：尚未提交。
- 目标：在不生成图片、不修改业务逻辑的前提下，确定豪门惊情 / 紫藤植物主题的视觉语言、信息架构和响应式布局。
- 修改：新增 `docs/DESIGN_SYSTEM.md`，定义色彩角色、字体、间距、私藏与公开组件语义、三端断点、可访问性和公共美术安全边界。
- 修改：新增 `docs/WIREFRAMES.md`，覆盖首页、房间大厅、游戏房间的电脑 / Pad / 手机九种布局及搜索、私藏、公开看板的关键状态。
- 修改：README 链接长期设计资料，并把当前阶段更新为“阶段 1 已完成”。
- 审稿材料：制作可切换页面和设备的交互式线框；该材料属于 Codex 会话视觉，不进入公开仓库。
- 关键决策：电脑游戏房间采用阶段 / 当前行动 / 公开看板三栏；Pad 采用状态置顶、双栏行动区和看板抽屉；手机采用当前任务优先和四项底部导航。
- 关键决策：私藏使用深梅紫封闭容器，公开线索使用象牙纸；正反面始终显示同一稳定编号，公开必须是独立确认动作。
- 验证：交互线框成功渲染；实际浏览器验证页面与设备切换、Pad 公开看板抽屉开关、手机私藏底栏切换均生效。
- 验证：公开安全关键词与尾随空白扫描通过。
- 未运行：未修改应用代码，因此本阶段没有重新运行 lint、运行时测试或生产构建；最近一次基线的 7 项运行时测试和生产构建均通过。
- 已知风险或阻塞：设计尚未在真实页面、真实长文本和 320px 设备上实现验证；交互审稿板不是生产组件。
- 唯一下一步：妹妹确认阶段 1 方向后，阶段 2 只重做首页与登录注册入口，再单独验收电脑、Pad、手机表现。

## 2026-08-28 — 建立跨设备交接文档并锁定视觉改版路线

- 基线提交：`afcbf7c3b3fcf478700f29a8cee86f63cd02aeba`
- 目标：读清远端项目结构，规划豪门惊情 / 紫藤植物主题的三端视觉改版，并建立长期维护文档。
- 已确认结构：Next.js 全栈单体；首页、房间大厅和游戏房间三层页面；SQLite 保存账号和房间状态；冻结剧本包由服务端按身份和阶段投影。
- 已确认问题：现有视觉偏通用暗色管理后台；Pad 缺少独立布局；游戏房间内容纵向堆叠；私藏线索和公开看板缺少稳定导航与鲜明区隔。
- 已决策：保留后端和权限模型，先做信息架构与视觉系统，再依次改首页、大厅和游戏房间。
- 已决策：电脑采用三栏可能性，Pad 采用双栏加抽屉，手机采用单栏加底部游戏导航。
- 已决策：原创图片只用于通用氛围主图、透明紫藤装饰和纹理；不使用任何真实剧情内容生成公开素材。
- 文档：新增 `README.md` 和本开发日志。
- 验证：`pnpm run test:runtime` 通过，7/7；`pnpm run build` 通过。
- 未运行：本轮没有修改页面代码，因此没有启动三端视觉测试。
- 唯一下一步：阶段 1，只输出视觉规范与首页、大厅、游戏房间的电脑 / Pad / 手机线框；不生成图片、不修改业务逻辑。

## 2026-08-27 — 创建公开仓库与首个界面原型

- 提交：`f2dc9b22a60416569fc9534e8e73e23714557336`
- 完成：创建 `Seeunever/wisteria-online` 公开仓库并推送 `main`。
- 完成：建立“暗格”首页、账号入口、搜证示意、私藏线索示意和公开线索看板原型。
- 边界：未提交原始剧本包、参考素材或依赖目录。
- 当时未完成：完整房间流程、真实剧本数据接入和最终页面验证。

---

## 新记录模板

复制下面内容到本文件顶部第一条日志之前，并删除不适用项：

```markdown
## YYYY-MM-DD — 简短标题

- 起始提交：`<sha>`
- 结束提交：`<sha 或尚未提交>`
- 目标：
- 修改：
- 关键决策：
- 验证：
- 未运行：
- 已知风险或阻塞：
- 唯一下一步：
```
