# 同行 · 在线剧本试玩

## 最新上线状态（2026-09-08）

用户最新要求：线上《应邪化仆》《紫藤夫人》分别开放一键测试辅助，支持单人从阅读走到最终揭晓。这是明确的逐本试玩例外，不自动给新本开放；设置见 `DEPLOYMENT.md`。紫藤六个角色均已通过全四幕单人测试和额外平票分支检查，使用 `node check-ziteng-assist.mjs` 复验。

正式入口为 `https://47.81.210.196/`，已替换旧站，使用共用访问口令＋用户名登录。正式存档与本地测试分开，测试代操作仅按上述逐本授权启用，开机启动和每小时存档备份保持开启。公网 HTTPS 与 Chrome 手机/平板尺寸入口已验收；真实手机异网游戏操作仍待用户检查。服务器目录、旧站归档和恢复方法见 `DEPLOYMENT.md`。下方较早条目中的“未部署”是当时的验收记录，以本节最新状态为准。

## 正式运行准备

正式模式、独立持久化存档、每小时在线备份和 HTTPS 代理配置已经接入，详见 `DEPLOYMENT.md` 与 `.env.production.example`。`node check-deployment.mjs` 验证配置、双端/重启保存、备份恢复和模拟手机断线恢复。实际部署与真实手机异网验收另行记录，不等同于本地测试通过。

## 单人测试辅助

这两个测试本选角后都有“测试中，帮我一键补完本轮其他人的选择和确认”按钮。只在点击时执行：补齐空位、替其他角色确认或投调查区域票、按既有顺序拿第一张合法牌；新牌默认公开（必须公开卡遵循原规则），原有隐藏牌保留，仍在轮末匿名发布。地点投票优先跟随本人的选择，否则选第一个合法区域。

遇到本人待操作、本轮结束或新幕阅读就停止；不会替本人拿牌、处理私人选择、确认、触发回忆或重置游戏。最终揭晓必须本人先明确确认。重复提交同一请求不会重复执行。注意：它也会代操作已经登录的其他账号，不要在与朋友正式游玩时使用。

`node server.mjs` 在默认本机地址启用；用 `$env:TEST_ASSIST='0'` 后启动即可关闭按钮及接口，非本机监听默认禁用。这里只添加两本专用测试辅助，不改变正式搜证引擎，也不作为其他剧本的默认功能。

验收：`node check-test-assist.mjs` 使用独立临时库，覆盖两本单人全流程、缺席补位、本人私人选择不被修改、轮次停止、最终确认、重复请求及关闭接口；Chrome 390px手机和820px平板实点按钮，截图已目检。没有用真实存档试点。

## 美术与界面

用户选择《明日方舟》方向的战术终端气质：深灰网格、黑白大色块、冷蓝操作按钮、少量警示黄、编号与中英文字级对比。首页使用原创SVG几何线稿，不复制官方标志、角色或界面素材；视觉参考 https://ak.hypergryph.com/ 。两本共用 `terminal.css`，只改变展示，原始剧本与卡片不裁切、不加滤镜。没有新增运行依赖、外部字体或远程图片请求。

样式验收：`node check-terminal.mjs` 检查320/390/820/1440px下首页、双本导航、登录选角、原图与触控尺寸；`node check-responsive.mjs` 覆盖应邪手机/平板/桌面及横屏；`node check-ziteng.mjs` 覆盖紫藤现有交互与即时隐藏/轮末发布。测试使用临时库；代表截图已目检。真实手机/iPad/Safari尚未验收。

## 剧本选择与紫藤夫人（2026-09-08）

首页现在先选剧本：`/yingxie/` 为原应邪化仆，`/ziteng/` 为紫藤夫人。两本角色与存档独立，原有应邪账号会话、进度和素材不迁移、不重置。每本仍用用户名登录，同名多设备接同一角色。

紫藤夫人独立接入6角色、4幕、48张普通卡，以及只属于乐婉的6张回忆。前三天集体投区域、按角色顺序取完该区域；第四天调查所有剩余地点，执行逐角色限制及条件牌。按用户最新要求，每人拿完立即选择隐藏/公开，选完交给下一人；公开选择暂存，轮末匿名统一发布。阶段推进采用用户确认的6人全员确认，酒馆外山上对吕松年允许调查。最终答题仍交还语音讨论，不恢复答案投票。

规则和映射：`ZITENG-FLOW.md`、`ZITENG-SOURCE-FACES.md`、私有 `private/ziteng/coverage.json`。首次导入 `node import-ziteng.mjs`，已有目录会拒绝覆盖；服务启动需要两本私有材料就绪。

验收 `node check-ziteng.mjs`：六账号完整四幕、权限、双端和重启、两本存档隔离；Chrome桌面/手机/平板模拟交互通过，代表截图已目检。原本第三幕及结局回归 `node check-third.mjs` 通过。未部署公网，未验证真实手机/iPad/Safari联网。

## 应邪化仆 · 已完成范围

已实现：用户名登录、同名多设备、角色独占、选角立即阅读自己的第一幕、全员确认与进度同步、重启保存。
五个角色都确认后进入第一幕两轮搜证：选地点挑牌背、按编号轮流、私读后最多隐藏一张。公开选择（含必须公开卡）先私下暂存，轮末统一发布，不显示领取者。第一轮结束直接开始第二轮，全部拿完后讨论；无轮间确认。

非行动者看不到实时牌池、他人选牌和隐藏选择。非搜证期间追加的公开选择，等下一次全员阶段确认统一发布。已有公开卡不回退。隐私检查覆盖接口、图片入口、观察者前后响应一致性及重启后的待公开状态。
第二幕已接入：第一幕讨论全员确认后开放第二幕原页，再全员确认开始第二幕一轮搜证；跨幕保留线索，隐藏额度仍为1张。
第三幕已接入：全员确认后读05–08页，再确认开始连续调查剩余合法线索；无人可拿时停止，末轮不足五人仍统一公开。独立“我的回忆”按钮按本人的关键词条件手动触发，原卡不能公开，不影响普通搜证。
收尾已接入：第三幕最终讨论后，每个角色明确确认结束；全员确认才开放“我的结局”和“公共真相”。结局按原页条件自行对照，可叠加，不自动判分。结束确认同时发布尚在队列里的普通线索，未选择公开的隐藏卡和回忆不自动公开。
主流程已接通至复盘。用户已决定不做内置投票：各剧本的题目和答案差异较大，公共与个人问题均由玩家语音讨论、口头回答或自行记录，再全员确认结束。未部署到互联网，也未做真实手机联网验收。

运行（Node 24，无新增第三方运行依赖）：

```powershell
cd E:\Wisteria\online-player
node server.mjs
```

在 Chrome 打开 http://127.0.0.1:4310 。默认仅本机可访问，未发布到互联网。
首次缺少 private/game.json 时运行 `node import-first-act.mjs`，从已认可的单机成品提取第一幕；不会导入全本或覆盖已有导入。

材料保存在忽略提交的 private/；账号与会话保存在忽略提交的 state/。服务没有通用静态文件入口，私页接口按登录角色检查权限。
同名即可登录是明确采用的熟人使用约定，不适用于陌生人开放注册。对外部署仍需用户另行授权，并配置 HTTPS 和 COOKIE_SECURE=1。

验证：`node check.mjs`。使用独立临时数据库，不占用试玩角色。已通过接口并发选角、同名同步、资源隔离、重启保留，以及 Chrome 桌面/390px 手机模拟同步阅读；已目检两种尺寸截图。未测试真实手机联网，未获用户实际验收。

全员确认已验证：未选角不能确认；同账号多端和重试不重复计数；4 人不推进，5 人才进入准备页；确认后仍可阅读；重启保留。使用新增独立表，不修改或清空已有账号与选角数据。

第一幕线索首次导入：`node import-search.mjs`，不覆盖现有素材。搜证验证：`node check-search.mjs`；Chrome 检查仍用 `node check.mjs`。测试使用独立临时数据。

第二幕首次导入：`node import-second-act.mjs`，不会覆盖已导入文件。`node check-search.mjs` 现覆盖两个幕的五账号流程与 Chrome 跨幕操作。15/25原文冲突经用户裁定，详见 SECOND-ACT-MAPPING.md。

第三幕首次导入：`node import-third-act.mjs`，不会覆盖已导入文件。`node check-third.mjs` 覆盖第三幕五人搜证及回忆隔离、同名两端与重启持久化；已用 Chrome 检查桌面和390px手机尺寸并目检截图。详见 THIRD-ACT-MAPPING.md。

收尾首次导入：`node import-ending.mjs`，复用5张个人结局双页、8张公共真相、1张结束规则，不覆盖原素材。触发词私有映射 `private/memory-triggers.json` 需随私有材料一起保留。

`node check-third.mjs` 现覆盖第三幕 → 最终讨论 → 全员结束 → 私人结局/公共真相：禁止提前揭晓、取消确认、4人不开放、同名两端去重、5人开放、权限隔离、重启保留、Chrome桌面/手机图片与操作，截图已目检。详见 ENDING-MAPPING.md。

范围决定：取消内置投票，不再将它列为待办；除非用户重新提出，不自动恢复开发。后续可复用 Skill 应区分多人同步机制与逐本核对的规则，不以投票作为完成前提。

可复用方法已整理到个人 Skill：`C:\Users\admin\.codex\skills\murder-mystery-online\SKILL.md`。本项目 `AGENTS.md` 提供读取入口；Skill 正文记录方法，本包特例放在其案例参考中，不作为其他剧本的默认规则。

手机与平板布局：600px及以下为单列卡片和全宽操作按钮，601–1100px卡片为两列，较宽桌面保留自动网格。长操作文案自动换行；回忆、结局弹窗使用固定标题/返回栏和独立滚动内容区，兼顾横屏、动态视口及安全区。原页不裁切，保留双指缩放及原图入口，不改变玩法和真实存档。

响应式检查：`node check-responsive.mjs`，独立临时数据库和临时端口，覆盖320/390px手机、768/820px平板、1024px平板横屏、844×390手机横屏和1280px桌面；检查登录、选角、牌背、隐藏/公开长按钮、原图、回忆、结局与真相，以及弹窗滚动和转屏后的勾选保留。Chrome模拟通过并目检代表截图；真实手机、iPad/Safari和联网仍需实际设备验收。
